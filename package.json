{
  "name": "whisprly",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "env -u LD_LIBRARY_PATH -u GTK_PATH -u GTK_EXE_PREFIX -u GDK_PIXBUF_MODULEDIR -u GDK_PIXBUF_MODULE_FILE -u GIO_MODULE_DIR -u GSETTINGS_SCHEMA_DIR -u LOCPATH -u WAYLAND_DISPLAY tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "firebase": "^12.12.1",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "@vitejs/plugin-react": "^6",
    "sharp": "^0.34.5",
    "typescript": "^5",
    "vite": "^8.0.11"
  }
}
